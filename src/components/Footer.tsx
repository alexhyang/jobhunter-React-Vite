import React from 'react';

export default function Footer() {
  const styles = {
    display: 'flex',
    justifyContent: 'center',
    margin: '30px 0',
  };

  return <div style={styles}>&copy; Alex Yang 2022</div>;
}
